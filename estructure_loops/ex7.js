//Faça um programa que leia 5 números e informe o maior número.
const input = require('prompt-sync') ({sigint:true});
let num = [];
if(Number(num) == num) {
    num = parseInt(num);
    for(let i = 0; i <5; i++) {
        num = input("Digite cinco números: ");
        console.log(num);
    }


}
